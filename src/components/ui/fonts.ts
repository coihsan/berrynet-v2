import { Sora, Archivo } from "next/font/google";

export const sora = Sora({
  subsets: ["latin"],
  display: "swap",
});

export const archivo = Archivo({
  subsets: ["latin"],
  display: "swap",
});
